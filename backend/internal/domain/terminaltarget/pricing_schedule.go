package terminaltarget

// Pricing schedule types and helpers live with the terminal-target domain
// records in routing_schedule.go so the runtime mirror and schedule contract
// remain indexed together. This file is retained as the named pricing seam;
// PricingWindowsDigest is the shared Go/SQL canonical digest implementation.
